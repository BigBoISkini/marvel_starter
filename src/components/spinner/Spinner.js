import logo from './logoHa.gif';

const Spinner = () => {
    return (
            <div style={{width: '100%' , height: '100%' , textAlign: 'center' , alignItems: 'center'}}>
                <img src={logo} alt="addsasd" style={{marginTop: '25px'}}></img>
            </div>

    )
}

export default Spinner;